# Notes

## 2026-05-09

- 프로젝트를 다시 시작했습니다.
- VS Code에서 venv를 추가했습니다.
- venv 안에 `pygame`을 설치했고 코드 실행을 확인했습니다.
- `AGENTS.md`를 프로젝트 작업 지침으로 사용하기로 했습니다.
- 먼저 `snake_game.py`를 개선하고 공통 게임 로직을 `snake_core.py`로 분리하기로 했습니다.
- 경로 탐색 봇은 기본 BFS, 꼬리 이동 반영, 장기 생존 전략 순서로 단계적으로 만들기로 했습니다.
- 유전 알고리즘 봇은 나중에 `snake_ga_bot.py` 파일로 따로 만들기로 했습니다.
- `snake_core.py`를 생성하고 보드 크기와 셀 크기 상수를 추가했습니다.

## 개발 메모

- ZIP으로 받은 프로젝트라면 `.git` 폴더가 없을 수 있습니다.
- GitHub와 변경 이력을 연결하려면 나중에 `git clone`으로 다시 받거나, 현재 폴더를 Git 저장소로 초기화해야 합니다.
